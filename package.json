{
  "name": "flip",
  "version": "0.1.0-alpha.1",
  "private": true,
  "scripts": {
    "dev": "cd vue-app && npm run dev",
    "build": "cd vue-app && npm run build",
    "preview": "cd vue-app && npm run preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
